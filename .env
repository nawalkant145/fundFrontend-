# Frontend env — leave VITE_API_URL empty so auto-detection works:
#   Local dev → http://localhost:5000/api/v1
#   Production build (Netlify/Firebase) → https://expglofundbackend.onrender.com/api/v1
# Only set this if you want to override the auto-detection.
# VITE_API_URL=http://localhost:5000/api/v1
